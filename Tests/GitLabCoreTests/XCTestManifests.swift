import XCTest

// No tests available for Swift 5.3 (all async/await requires Swift 5.5+)
class GitLabCoreNoTests: XCTestCase {
    func testPlaceholder() {
        XCTAssertTrue(true, "Tests require Swift 5.5+ with async/await support")
    }
}
